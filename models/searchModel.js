// const conn = require("../config/dbConfig");

// exports.searchProduct = async (searchTerm) => {
//   const sql = `SELECT * FROM products WHERE keywords LIKE ?`;
//   const [rows] = await conn.execute(sql, [`%${searchTerm}%`]);
//   return rows;
// };
